function BestAudioSource(string "source", int "track", bool "exactsamples", string "varprefix")
{
LoadPlugin(AviSynthPlugins64PDir + "mt/BestAudioSource.dll")
        BestAudioSource(source, track, exactsamples, varprefix)
}