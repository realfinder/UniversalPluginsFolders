function chkdr_grain(clip c, float "sigma", int "res", float "rad", float "dev", int "seed", int "cf", int "cp", int "draft", int "cpuopt")
{
LoadPlugin(AviSynthPlugins64PDir + "36x/chickendream.dll")
        chkdr_grain(c, sigma, res, rad, dev, seed, cf, cp, draft, cpuopt)
}