function yadifmod2(clip clip, int "order", int "field", int "mode", clip "edeint", int "opt")
{
LoadPlugin(AviSynthPlugins64Dir + "26/yadifmod2.dll")
        yadifmod2(clip, order, field, mode, edeint, opt)
}