function GraMaMa(clip clip, int "mode", int "a", int "b", int "rad", int "rad2", bool "binarize")
{
LoadPlugin(AviSynthPluginsDir + "25/GraMaMa.dll")
        GraMaMa(clip, mode, a, b, rad, rad2, binarize)
}