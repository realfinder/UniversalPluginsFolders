function IT(clip clip, int "fps", int "threshold", int "pthreshold", string "ref", bool "blend", bool "debug", string "read", string "write", string "log", int "dimode")
{
LoadPlugin(AviSynthPluginsDir + "25/IT.dll")
        IT(clip, fps, threshold, pthreshold, ref, blend, debug, read, write, log, dimode)
}