function cc(clip clip, int "y1", int "y2", int "c1", int "c2", bool "interlaced", float "yc", bool "ylimit", bool "climit") 
{
LoadPlugin(AviSynthPluginsDir + "25/cc.dll")
        cc(clip, y1, y2, c1, c2, interlaced, yc, ylimit, climit) 
}