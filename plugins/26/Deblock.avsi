function Deblock(clip clip, int "quant", int "aOffset", int "bOffset")
{
LoadPlugin(AviSynthPluginsDir + "26/Deblock.dll")
        Deblock(clip, quant, aOffset, bOffset)
}

function Deblock_deblock(clip clip, int "quant", int "aOffset", int "bOffset")
{
LoadPlugin(AviSynthPluginsDir + "26/Deblock.dll")
        Deblock(clip, quant, aOffset, bOffset)
}