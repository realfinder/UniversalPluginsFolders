function Info2(clip clip, int "size", string "font", int "background", bool "verbose", bool "glyph")
{
LoadPlugin(AviSynthPluginsDir + "26/Info2.dll")
        Info2(clip, size, font, background, verbose, glyph)
}