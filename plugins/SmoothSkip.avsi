function SmoothSkip(Clip Clip, Clip altClip, int "cycle", int "create", float "offset", int "scene", bool "debug")  {
LoadPlugin(AviSynthPluginsDir + "26/SmoothSkip.dll")
    SmoothSkip(Clip, altClip, cycle, create, offset, scene, debug)
}