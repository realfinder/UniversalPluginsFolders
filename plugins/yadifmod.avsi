function yadifmod(clip clip, int "order", int "field", int "mode", clip "edeint", int "opt")
{
LoadPlugin(AviSynthPluginsDir + "25/yadifmod.dll")
        yadifmod(clip, order, field, mode, edeint, opt)
}